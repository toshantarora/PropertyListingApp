<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.propertylisting">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-feature android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.UpdateSocietyActivity" />
        <activity android:name=".activities.UpdateScoActivity" />
        <activity android:name=".activities.UpdateKothiActivity" />
        <activity android:name=".activities.UpdateHBActivity" />
        <activity android:name=".activities.UpdateBoothActivity" />

        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.SocietyListActivity" />
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.SearchSocietyActivity" />
        <activity android:name=".activities.SearchScoActivity" />
        <activity android:name=".activities.SearchKothiActivity" />
        <activity android:name=".activities.SearchHBActivity" />
        <activity android:name=".activities.SearchBoothActivity" />
        <activity android:name=".activities.ScoListActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.KothiListActivity" />
        <activity android:name=".activities.HousingBoardListActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.BoothListActivity" />
        <activity android:name=".activities.AddSocietyActivity" />
        <activity android:name=".activities.AddScoActivity" />
        <activity android:name=".activities.AddKothiActivity" />
        <activity android:name=".activities.AddHousingBoardActivity" />
        <activity android:name=".activities.AddBoothActivity" />
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>